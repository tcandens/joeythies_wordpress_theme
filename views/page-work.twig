{% extends 'base.twig' %}

{% block content %}
  <div class="">
    <h1>{{ title }}</h1>
  </div>
  <div class="s-darkfield">
    {% for post in posts %}
      {% include 'work-preview.twig' %}
    {% endfor %}
  </div>
{% endblock %}
