{% extends 'base.twig' %}

{% block content %}
  <div class="s-darkfield">
    <h1>{{ title }}</h1>
  </div>
  <div class="o-col o-center">
    {% for post in posts %}
      {% include 'post-preview.twig' %}
    {% endfor %}
  </div>
{% endblock %}

