/**************
 * BASE STYLES
 **************/

.flex-container
  display flex
.flex-center
  @extend .flex-container
  flex-direction column
  align-items center
.outline-box
  position relative
  margin -1em 0 0 -1em
  padding 1em
  border 0.5em solid MAINCOLOR
  background-color white
.bordered
  border 8px solid white
.line-shadow
  max-width 30em
  top 1em
  left 1em
  padding 0 1em 1em 0
  margin 1em 0 1em 1em
  lines(BLUE, 3px, 11px, -45)
.window
  border-radius 1em 1em 0 0
  background-color white
  padding 1.2em 0 0 0
  border 0.5em solid white
  border-top 0
  &:before
    display block
    content ''
    position relative
    top -0.5em
    left 0.5em
    width 0.4em * 8
    height 1em
    pip-radius = 0.4em
    x = pip-radius
    background-image radial-gradient(circle at x 0.5em, RED, RED pip-radius, transparent pip-radius), radial-gradient(circle at x*4 0.5em, RED, RED pip-radius, transparent pip-radius), radial-gradient(circle at x*7 0.5em, RED, RED pip-radius, transparent pip-radius)
